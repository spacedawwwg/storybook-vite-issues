import React from 'react';
import tokens from '@project-name/core-tokens';

export default function TypeBlock() {
  return <div style={{ background: tokens.color.brand['project-name-blue'].value, height: 100 }} />;
}
